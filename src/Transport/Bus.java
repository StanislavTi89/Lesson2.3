package Transport;

public class Bus extends Transport {

    public Bus (String brand, String model, double engineCapacity, String color,
               int year, int maxSpeed, String country, String transmission, String bodyType,
               String regNumber, int numberSeats) {
        super (brand,model,year,color,country,maxSpeed);
    }
}
